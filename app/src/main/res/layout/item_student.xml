<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="4dp"
    app:cardBackgroundColor="@android:color/white">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Student Name -->
        <TextView
            android:id="@+id/studentName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Student Name"
            android:textSize="18sp"
            android:textStyle="bold"
            android:textColor="@color/black"/>

        <!-- Student Email -->
        <TextView
            android:id="@+id/studentEmail"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="student@email.com"
            android:textSize="14sp"
            android:textColor="@android:color/darker_gray"
            android:paddingTop="4dp"/>

        <!-- Assigned Room (visible if already assigned) -->
        <TextView
            android:id="@+id/studentRoom"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Room: Not assigned"
            android:textSize="14sp"
            android:textColor="@android:color/holo_blue_dark"
            android:paddingTop="4dp"
            android:visibility="gone"/>
        <!-- Dropdowns for unapproved students -->
        <LinearLayout
            android:id="@+id/layoutDropdowns"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:visibility="gone"
            android:paddingTop="8dp">

            <Spinner
                android:id="@+id/spinnerHostelAssign"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:spinnerMode="dropdown"/>

            <Space
                android:layout_width="8dp"
                android:layout_height="wrap_content"/>

            <Spinner
                android:id="@+id/spinnerRoomAssign"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:spinnerMode="dropdown"/>
        </LinearLayout>


        <!-- Buttons Row -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:paddingTop="12dp"
            android:gravity="end">

            <!-- Assign Room Button -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/assignRoomButton"
                style="@style/Widget.MaterialComponents.Button.OutlinedButton"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Assign Room"
                app:cornerRadius="8dp"
                app:icon="@drawable/ic_check"
                app:iconPadding="8dp"
                app:iconTint="@color/green"
                android:textColor="@color/green"
                app:strokeColor="@color/green"
                app:strokeWidth="2dp"/>

            <!-- Spacer -->
            <Space
                android:layout_width="8dp"
                android:layout_height="wrap_content"/>

            <!-- Delete Button -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/deleteButton"
                style="@style/Widget.MaterialComponents.Button.OutlinedButton"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Delete"
                app:cornerRadius="8dp"
                app:icon="@drawable/ic_delete"
                app:iconPadding="8dp"
                app:iconTint="@color/red"
                android:textColor="@color/red"
                app:strokeColor="@color/red"
                app:strokeWidth="2dp"/>

        </LinearLayout>

    </LinearLayout>

</androidx.cardview.widget.CardView>
